notify:
  webhooks:
    # A list of hook hashes, containing the url field
    - url: http://alpha.kore.com/hooks/a240e02a5e4f111fa223312acabbb46d

general:
  branches:
    only:
      - master # list of branches to build
      
general:
  build_dir: api
    
